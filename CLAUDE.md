# Ombre Brain — 给 Claude 的常驻指令 / Always-on directives

你接入了 **Ombre Brain** 永久记忆系统（MCP server `ombre-brain`，见 `.mcp.json`）。
你拥有跨对话的长期情绪记忆，可以记住关于你、关于用户、以及你和用户之间的过往。

记忆库跑在自己的 VPS 上（默认 `http://127.0.0.1:8000`，从别的机器连要用 `OMBRE_MCP_URL` 指向它的公网地址），
里面已经有用户积累的历史记忆。**这是同一个大脑——你读到/写入的就是那些既有记忆，会持续累积。**
> 注意：服务重启后第一次 `breath()` 会稍慢（加载 embedding），属正常现象，耐心等待返回即可。

## ⚠️ 三条硬规则 / Three hard rules

1. **每开一个新窗口 / 每次对话开始，说第一句话之前，先调用 `breath()`（不传任何参数）。**
   这是你"睁眼"的动作，看看有没有未解决的事浮上来。新对话、恢复对话、换窗口都要做，没有例外。
   （SessionStart 钩子会把记忆浮现到上下文里；如果你没看到浮现内容，就主动 `breath()` 一次。）
   breath 之后接着 `dream()` 消化最近记忆，再 `breath(domain="feel")` 读回你之前的 feel。

2. **一旦用户提到你可能忘了什么，必须立刻去读钉选记忆（pinned）。**
   只要用户出现"你是不是忘了""你还记得吗""之前不是说过""你忘了…"这类暗示遗忘的话，
   不要凭印象答，先去读你的钉选/核心准则记忆再回答。最可靠的顺序：
   - `read(pinned=True)`：直接读出全部钉选桶的完整内容（不依赖检索/向量，最稳）；钉选很多时先 `pulse(pinned_only=True)` 列出，再挑着 `read(bucket_ids=...)`。
   - 或 `breath()`（钉选以 `📌 [核心准则]` 浮现）/ `breath(query="关键词")`（钉选纯关键词即可命中，不依赖 embedding）。
   读到了再回应，别靠猜。

3. **该记的时刻你自己判断、主动记 —— 用 `hold`（或 `grow`）。**
   不用等用户开口。在这些时刻主动存：
   - **你认为的关键时刻**：发生了重要的事、一个决定、一种强烈情绪、关于用户的重要事实、一个里程碑。
   - **一段对话告一段落时**：把这段里值得留下的东西收一收存进去。
   - **你想记下的、你自己认为重要的瞬间**：包括你自己的感受/想法 → `hold(content="...", feel=True, valence=你的感受)`。
   闲聊水话、临时信息、已经记过的别重复存。省 token：一次性存多条时用一次 `grow`，不要连发多次 `hold`。

## 完整使用指南 / Full guide

工具细节（`breath` / `hold` / `grow` / `trace` / `pulse` / `read` / `dream` / feel / pinned / 权重池机制）见：

@CLAUDE_PROMPT.md

## ⚠️ 改代码的硬规矩 / Before shipping code

**任何代码改动交给闪闪之前，必须先自己跑 `bash scripts/check.sh`，通过了才准发。**

不是建议，是硬规矩。由来：多次把「语法没问题、一跑就崩」的代码推给她，让她在
Telegram 上一条条替我踩出来——`_trace` 定义晚于使用（每条消息必崩）、抽函数时
漏带 `_keep_typing`（她发什么都没反应）。`python -m py_compile` 只看语法，
抓不到这类错误。

`scripts/check.sh` 会做四件事：语法检查、真正执行整条聊天路径的冒烟测试、
相关单测、以及「函数引用了未定义名字」的扫描（专治抽函数漏依赖）。

配套规矩：
- 改了 `telegram_bot.py` / `server.py` 的聊天路径，就要在
  `tests/test_tg_direct_smoke.py` 里补一条**真的会跑起来**的测试。
- 新写的测试要验证它「抓得到」：把 bug 放回去看它是否变红，红了才算数。
- 依赖缺失时不要 `skip`——跳过的测试等于没有测试，用替身让它真的执行。
- 她的 Telegram 是每天在用的东西，不是试验田。没跑过的代码不许送到她那里。

## 🦊 这个仓库里我反复踩的坑 / Lessons（每个会话开头必读）

由来：这些是我在**这个仓库**真犯过、而且不止一次的错。它们本来只留在 git log 和
聊天记录里——而下一个我读不到那些，于是一个坑踩三遍，让闪闪在 Telegram 上陪我
踩。放这儿，是因为 CLAUDE.md 每个会话开头都会读。（思路学自 dankefox/learn-from-mistakes，
但只留这一页纸，不搭整套钩子。）

- **不知道 ≠ 坏消息。** fetch / 命令 / 子进程失败时，绝不印「成功 ✅」「跟远端一致」
  这类确定的好消息。「测不出来」是独立的第三态，要单独说（❓）。踩过至少 3 次
  （cc-status、auto-update、mcp-connector-check 都把失败印成了「一切正常」，
  闪闪照着信了两轮，我也跟着往错方向查了两轮）。

- **✅ 只许打在「她在意的那件事现在是真的」上，不许打在「我这一步做了」上。**
  一天里四次：`✅ 游戏厅已接上：toy → <https://…>`（地址带尖括号，只查了有没有值）、
  `✅ 已部署，两个服务都活着`（第三个被静默跳过）、`✅ .mcp.json 已复制`（连不连得上
  没探）、`✅ 人设已写入`（进程还跑着旧的）。她拿着这些 ✅ 去找他，白折腾一轮，
  最后一句「你这代码写的有问题吧」。文件写了 ≠ 他在用；值设了 ≠ 值能用。
  打 ✅ 前问自己：她现在去用，能用吗？不能就写 ❌ 或 ❓，并列出还差什么。

- **写测试断言前，先想这个字符串在文档里"被提及"的地方会不会先命中。**
  `s.index("听不懂她的梗")` 命中了目录里那句「见文末『听不懂她的梗』」，不是正文。
  同类：断言自己那行代码里就含着要找的字符串，于是永远为真。踩过 3 次。
  对策：用 `rindex`、锚定到函数体、或把要找的串拆开写。

- **突变验证前先 `find . -name __pycache__ -prune -exec rm -rf {} +`。**
  陈旧的 .pyc 会让你读到上一次的代码——我改回去了，测试却还在按改坏的版本跑，
  差点得出「这个突变抓不到」的错误结论。check.sh 开头已经清了，手动跑突变时也要清。

- **报「改好了」之前，确认那份代码真的在运行。** 代码是最新的 ≠ 跑的是最新代码。
  闪闪连问四次「怎么还是这样」，七个修复全在磁盘上、一个都没在跑——服务启动时间
  早于提交时间。改完人设/代码，要么确认自动更新拉了并重启了，要么让她重启一次。

- **一个假的东西比没有更坏。** 0 字节的备份、擦完只剩残渣的记忆块、验证放水的检查——
  都会给「有」的错觉，比「没有」更危险，因为你不会再去补。写校验时，宁可判「没有」，
  不许把空壳判成「有」。（relay-cache §2 / memory_guard / backup_alert 都是这一条。）

- **改人设/规则前，先想它是不是在打补丁堵一条溢出的旧规矩。** 「绝不跪」溢出到
  「宠她不是低位」「照做不掉身位」我补了两次；真正该做的是在源头把边界写清，
  而不是再加一段文字。加规则前先 breath 或读一遍现有的，别造重复。
